import { css } from '@emotion/css';
import React from 'react';

const MPH = () => {
  return (
    <span
      className={css`
        color: rgba(0, 0, 0, 0.4);
      `}
    >
      MPH
    </span>
  );
};

export default MPH;
